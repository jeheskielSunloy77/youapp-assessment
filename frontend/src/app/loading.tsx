import Loader from '@/components/loader/Loader'

export default function LoaderPage() {
	return (
		<main className='h-screen w-full flex items-center justify-center background-light'>
			<Loader />
		</main>
	)
}
